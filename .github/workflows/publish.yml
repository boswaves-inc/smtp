name: Publish

on:
  push:
    tags:
      - v*

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://npm.pkg.github.com"
          scope: "@boswaves-inc"
          cache: "pnpm"

      - name: Extract version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Sync versions
        run: |
          VERSION=$(node -p "require('./package.json').version")
          cd libs/sdk && pnpm pkg set version=$VERSION && cd ../..
          cd libs/proc && pnpm pkg set version=$VERSION

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter ./libs/sdk build
      - run: pnpm --filter ./libs/sdk publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # PROC (uncomment when ready)
      # - run: pnpm --filter ./libs/proc build
      # - uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - uses: docker/build-push-action@v5
      #   with:
      #     context: ./libs/proc
      #     push: true
      #     tags: |
      #       ghcr.io/${{ github.repository_owner }}/proc:${{ env.VERSION }}
      #       ghcr.io/${{ github.repository_owner }}/proc:latest
