### 1. Login to pnpm with github username and classic token
go to the root folder and run the following command

```bash
pnpm login --registry=https://npm.pkg.github.com --scope=@boswaves-inc
```

### 2. Install all the packages
go to the root folder and run the following command

```bash
pnpm i -D
```
